export function CreateCourseValidate(a, b, c, d, e, f, g, h,i,j) {
  if (
    a.length === 0 ||
    b.length === 0 ||
    c.length === 0 ||
    d.length === 0 ||
    e.length === 0 ||
    f.length === 0 ||
    g.length === 0 ||
    h.length === 0 ||
    i.length === 0 ||
    j.length === 0 
    
  ) {
    return "No input field should be empty!!";
  }
}

  